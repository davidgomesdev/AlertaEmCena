#[allow(dead_code)]
pub struct Event {
    pub event_type: String,
    pub title: String,
    pub subtitle: String,
    pub description: String,
    pub link: String,
    pub start_dates: String,
    pub last_dates: String,
}
